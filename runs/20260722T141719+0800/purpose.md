# Purpose

## 目标

让 FastAPI 示例的调用方能够用正整数用户 ID 查询一个现有用户，并对不存在的用户获得明确的 404 响应。

## 范围

- 增加 `GET /users/{user_id}`。
- 复用当前内存用户数据。
- 增加目标接口的单元测试。
- 不增加持久化、写接口、认证或其他功能。

## 成功标准

- 现有用户返回 HTTP 200，正文包含 `id`、`username`、`email`。
- 不存在的用户返回 HTTP 404。
- 路径参数拒绝零、负数及非整数。
- 既有列表接口与新增接口的单元测试均通过。
